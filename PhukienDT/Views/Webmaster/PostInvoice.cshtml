
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout_Webmaster.cshtml";
}
@section Scripts{
    <script src="/Scripts/app/webmaster/postinvoice.js"></script>
    <script>
        var postjs = new postinvoiceController();
        postjs.initialize();
    </script>
}

<div class="page-title">
    <div class="title_left">
        <h3>Hóa đơn mua tin</h3>
    </div>
    <div class="title_right">
        <div class="col-md-5 col-sm-5 col-xs-12 form-group pull-right">
            <div class="input-group">
                <input type="text" id="txtKeyword" class="form-control" placeholder="Gõ từ khóa...">
                <span class="input-group-btn">
                    <button class="btn btn-primary" id="btnSearch" type="button">Tìm!</button>
                </span>
            </div>
        </div>
        <div class="col-md-2 col-sm-12  form-group pull-right">
            @*<button id="btnCreate" class="btn btn-success">Thêm mới</button>*@
        </div>
    </div>
</div>
<div class="clearfix"></div>
<div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_content">
                <div class="panel panel-default table-responsive">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th width="5%">Mã HD</th>
                                <th>Nhà cung cấp</th>
                                <th>Gói tin</th>
                                <th>Thời gian</th>
                                <th>Trạng thái</th>
                                <th width="10%">#</th>
                            </tr>
                        </thead>
                        <tbody id="tbl-content"></tbody>
                    </table>
                </div>
                <!--Pagination-->
                <div class="row">
                    <div class="col-sm-5">
                        <div class="dataTables_info" id="datatable-checkbox_info" role="status" aria-live="polite">
                            <select id="ddlShowPage">
                                <option value="10" selected="selected">10</option>
                                <option value="20">20</option>
                                <option value="30">30</option>
                                <option value="40">40</option>
                            </select>
                            <span class="item-per-page">
                                bản ghi/trang.
                            </span>
                            Tổng số bản ghi: <strong id="lblTotalRecords"></strong>
                        </div>
                    </div><div class="col-sm-7">
                        <div class="dataTables_paginate paging_simple_numbers" id="datatable-checkbox_paginate">
                            <ul id="paginationUL"></ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<script id="table-template" type="x-tmpl-mustache">
    <tr>
        <td>{{MaHD}}</td>
        <td>{{NCC}}</td>
        <td>{{Type}}</td>
        <td>{{Time}}</td>
        <td>{{{Status}}}</td>
        <th scope="row">
            <button class="btn btn-md btn-success btn-edit" data-id="{{KeyId}}" type="button" data-toggle="tooltip" data-placement="top" title="Duyệt" {{{IsCompleted}}}><i class="fa fa-pencil"></i></button>
        </th>
    </tr>
</script>
